# Downloaded workflow references

These workflows were downloaded for structural analysis. They are preserved
unchanged and are not CFP production fixtures.

```text
High_Quality_GGUF.json
Trellis2Multiviews_GGUF.json
```

They depend on node packs and CUDA-oriented settings that are not available in
the current M3 Max ComfyUI environment. See
`docs/downloaded_trellis2_examples.md` before attempting to run them.
